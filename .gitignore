__pycache__/
*.pyc
.tree_cache.pt
logs/
models/
plots/

# Training data and test data — not shipped via git.
# Users supply their own training CSVs and test workloads.
# Use root-anchored paths so we don't accidentally ignore orig_train/train/data/.
/data/
/test_workloads/
/gpt_results/
/best_fixed_results/

# Diagnostic / debug scripts.
diag_emb.py
diag_emb_trained.py
inspect_gate_dist.py
