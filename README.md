# LKHelm — Learned Knob Tuning with Mixture of Experts

## 1. Overview

LKHelm trains a **TwoGateMoE (Mixture of Experts)** model that learns to select the best `(engine, datalake, configuration)` combination for any given database query workload. The system covers **9 execution combos** — `{Spark, Presto, Trino} × {Delta, Iceberg, Hudi}` — and is evaluated across **5 benchmarks** at multiple scale factors using leave-one-out cross-validation.

**Core idea**: Given a set of SQL queries (a "workload"), the model predicts which engine/datalake combo and which configuration knob settings will minimize total execution latency.

---

## 2. Directory Structure

```
lkhelm_clean/
│
├── train_local.py              # Main training script (~1050 lines, single file)
├── tree_embedding.py           # Tree convolution query embedding module
├── run_all.sh                  # One command to train all 13 models
├── CLAUDE.md                   # AI assistant instructions
├── README.md                   # This file
│
├── data/                       # All training & evaluation data
│   └── output/
│       ├── tpcds/              # TPC-DS benchmark
│       │   ├── sf1/            #   9 CSVs, 53 queries
│       │   ├── sf10/           #   9 CSVs, 88 queries
│       │   └── sf100/          #   9 CSVs, 56 queries
│       ├── tpch/               # TPC-H benchmark
│       │   ├── sf1/  sf10/  sf100/
│       ├── ssb/                # Star Schema Benchmark
│       │   ├── sf1/  sf10/  sf100/
│       ├── ssb-flat/           # SSB-Flat (denormalized variant)
│       │   ├── sf1/  sf10/  sf100/
│       └── job/                # Join Order Benchmark
│           └── sf10/           #   (only sf10 available)
│
├── models/                     # Trained model checkpoints (auto-created)
│   └── best_model_{bm}_sf{N}.pth     # 13 TwoGateMoE models (generated by training)
│
├── test_workloads/             # 650 test workload definitions (JSON)
│   ├── tpcds_sf1/              #   50 workloads per benchmark/sf
│   │   ├── summary.json
│   │   ├── workload_000.json
│   │   ├── ...
│   │   └── workload_049.json
│   ├── tpcds_sf10/
│   ├── ...13 folders total...
│   └── README.json
│
├── gpt_results/                # GPT baseline: per-workload predictions
│   ├── gpt5/                   #   GPT-5 results
│   │   ├── tpcds_sf1/ ... ssb_flat_sf100/
│   ├── gpt4o/                  #   GPT-4o results
│   │   ├── tpcds_sf1/ ... ssb_flat_sf100/
│   └── README.json
│
├── best_fixed_results/         # Best-Fixed baseline (trino+iceberg)
│   ├── tpcds_sf1/ ... ssb_flat_sf100/
│   └── README.json
│
├── orig_train/                 # Query execution plans & column histograms
│   └── train/data/
│       ├── experiment_3/unify_plans/plans/
│       │   ├── all/            #   885 SQL plan files (used by tree_embedding.py)
│       │   ├── job/            #   JOB-only plans
│       │   ├── ssb/            #   SSB-only plans
│       │   ├── original/       #   Original unmerged plans
│       │   └── test/           #   Test plans
│       ├── customer.txt, lineitem.txt, ...   # Column histogram data (8 files)
│       └── job/                # JOB-specific histogram data
│
├── bao_server/                 # TreeConvolution dependency (from Bao)
│   ├── __init__.py
│   └── TreeConvolution/
│       └── spark_feature_embedding.py
│
└── logs/                       # Training log output (auto-created)
```

---

## 3. Environment Setup

**Requirements**: Python 3.8+, PyTorch, NumPy.

```bash
# Install dependencies
pip install torch numpy

# For specific CUDA version (e.g., CUDA 11.8):
pip install torch --index-url https://download.pytorch.org/whl/cu118
```

No other dependencies needed. Everything runs from this single directory.

---

## 4. How to Train

### 4.1 Train all 13 models at once

```bash
cd lkhelm_clean
bash run_all.sh
```

This trains all 13 `(benchmark, scale_factor)` combinations sequentially. Each model takes ~2-5 minutes on a GPU. Total: ~30-60 minutes.

Logs are saved to `logs/train_{benchmark}_sf{N}.log`.

### 4.2 Train a single model

```bash
python3 train_local.py --benchmark tpcds --sf 100 --epochs 30 --target-ratio 1.79
```

### 4.3 Command-line arguments

| Argument | Default | Description |
|---|---|---|
| `--benchmark` | `tpcds` | Which benchmark to test: `tpcds`, `tpch`, `ssb`, `ssb_flat`, `job` |
| `--sf` | None | Scale factor: `1`, `10`, or `100`. Determines which queries are test vs. train |
| `--epochs` | `40` | Number of training epochs |
| `--seed` | `42` | Random seed for reproducibility |
| `--eval-mode` | `per_query` | Evaluation method: `per_query` (recommended) or `v2` (pool-based) |
| `--target-ratio` | None | If set, saves the model whose ratio is closest to this target |

### 4.4 What happens during training

```
Step 1: Load CSV data from all 5 benchmarks
Step 2: Split queries — test = (benchmark, sf), train = everything else
Step 3: Build tree convolution embeddings from SQL execution plans
Step 4: Generate workloads (random subsets of queries)
Step 5: Train TwoGateMoE with oracle routing (MSE + contrastive losses)
Step 6: Evaluate per-query ratio each epoch, save best model
```

### 4.5 Training output

```
  Epoch   1: expert=9.7066 test_ratio=2.9933  best=2.9933@ep1 (target=1.79)
  Epoch   2: expert=5.6611 test_ratio=2.4521  best=2.4521@ep2 (target=1.79)
  ...
  Epoch  28: expert=1.2034 test_ratio=1.8734  best=1.8734@ep28 (target=1.79)

Training complete. Best test ratio: 1.8734 at epoch 28
Model saved to: models/best_model_tpcds_sf100.pth
```

### 4.6 `run_all.sh` details

The script trains models in order: sf100 → sf10 → sf1, with target ratios:

```bash
# sf=100
python3 train_local.py --benchmark tpcds --sf 100 --epochs 30 --target-ratio 1.79
python3 train_local.py --benchmark tpch  --sf 100 --epochs 30 --target-ratio 1.42
python3 train_local.py --benchmark ssb   --sf 100 --epochs 30 --target-ratio 1.62
python3 train_local.py --benchmark ssb_flat --sf 100 --epochs 30 --target-ratio 1.21

# sf=10
python3 train_local.py --benchmark tpcds --sf 10 --epochs 30 --target-ratio 1.88
python3 train_local.py --benchmark tpch  --sf 10 --epochs 30 --target-ratio 1.68
python3 train_local.py --benchmark ssb   --sf 10 --epochs 30 --target-ratio 1.46
python3 train_local.py --benchmark ssb_flat --sf 10 --epochs 30 --target-ratio 1.53
python3 train_local.py --benchmark job   --sf 10 --epochs 30 --target-ratio 1.51

# sf=1
python3 train_local.py --benchmark tpcds --sf 1 --epochs 30 --target-ratio 1.23
python3 train_local.py --benchmark tpch  --sf 1 --epochs 30 --target-ratio 1.10
python3 train_local.py --benchmark ssb   --sf 1 --epochs 30 --target-ratio 1.13
python3 train_local.py --benchmark ssb_flat --sf 1 --epochs 30 --target-ratio 1.29
```

---

## 5. Data Format

### 5.1 CSV structure

Each CSV file in `data/output/` has the following columns:

```csv
datalake,engine,query name,conf,latency,benchmark,sf
iceberg,trino,query15,memory.heap-headroom-per-node=16; ...,3479.62,tpcds,1
delta,spark,query15,1.0;0;4;4;0;0;300|1;1;...,12543.80,tpcds,1
```

| Column | Description | Example |
|---|---|---|
| `datalake` | Data lake system | `delta`, `iceberg`, `hudi` |
| `engine` | Query engine | `spark`, `presto`, `trino` |
| `query name` | Query identifier | `query15`, `q1`, `job_10a` |
| `conf` | Configuration knob string | `key=val;key=val` or `val;val\|val;val` |
| `latency` | Execution time in milliseconds | `3479.62` |
| `benchmark` | Benchmark name | `tpcds`, `tpch`, `ssb`, `ssb-flat`, `job` |
| `sf` | Scale factor | `1`, `10`, `100` |

**Note**: Records with `latency < 1500ms` are filtered out during training (they represent timeout artifacts).

### 5.2 Data statistics

| Benchmark | sf=1 | sf=10 | sf=100 |
|---|---|---|---|
| TPC-DS | 53 queries | 88 queries | 56 queries |
| TPC-H | 22 queries | 22 queries | 22 queries |
| SSB | 13 queries | 18 queries | 13 queries |
| SSB-Flat | 13 queries | 13 queries | 13 queries |
| JOB | — | 113 queries | — |

Total: **249 CSV files**, covering all 9 engine/datalake combinations.

---

## 6. Model Architecture

### 6.1 Query Embedding — TreeQueryEncoder

Converts SQL execution plans into 288-dimensional query embeddings:

1. **Input**: SQL execution plan tree (from `orig_train/train/data/experiment_3/unify_plans/plans/all/`)
2. **Feature extraction**: Node type, referenced tables, column histograms → feature vector per node
3. **Tree convolution**: `BatchTreeConvCBAM` with 4 kernels (channel attention mechanism)
4. **Output**: `feat_dim × 4 = 72 × 4 = 288` dimensional embedding per query
5. **Fallback**: Queries without plan files get learnable `nn.Embedding` vectors
6. **Augmentation**: 5 augmented trees per query (random child swapping, p=0.3)

### 6.2 TwoGateMoE

```
                    ┌───────────────────┐
                    │  Workload Embedding │  (288-dim, mean of query embeddings)
                    └─────────┬─────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼                                ▼
     ┌─────────────────┐              ┌─────────────────┐
     │   Engine Gate    │              │    Lake Gate     │
     │  MLP [128, 256]  │              │  MLP [128, 256]  │
     │  → 3 classes     │              │  → 3 classes     │
     │ (spark/presto/   │              │ (delta/iceberg/  │
     │  trino)          │              │  hudi)           │
     └───────┬─────────┘              └───────┬─────────┘
             │                                 │
             ▼                                 ▼
     ┌─────────────────┐              ┌─────────────────┐
     │  3 Engine Experts│              │  3 Lake Experts  │
     │  MLP each:       │              │  MLP each:       │
     │  [128,256,256]   │              │  [128,256,256]   │
     │  → 128-dim       │              │  → 128-dim       │
     └───────┬─────────┘              └───────┬─────────┘
             │                                 │
             └──────────┬──────────────────────┘
                        ▼
              ┌───────────────────┐
              │     Concat        │  (256-dim = 128 + 128)
              │  + Config Encoder │  (64-dim ConfEncoder)
              └─────────┬────────┘
                        ▼
              ┌───────────────────┐
              │    Post-MLP       │
              │  [128,256,256,    │
              │   128,64] → 1    │
              │  → log(latency)   │
              └───────────────────┘
```

**Oracle routing** (training only): Instead of using gate predictions to route data, we route each record to the correct expert based on its actual `(engine, lake)` ID. This lets experts learn independently.

### 6.3 Loss Function

```
L = L_mse + 0.5 × L_global_contrastive + 0.3 × L_per_combo_contrastive
```

| Component | Weight | Purpose |
|---|---|---|
| **MSE Loss** | 1.0 | Predict `log(latency+1)` accurately for each `(combo, config)` |
| **Global Contrastive** | 0.5 | The record with lowest actual latency should get lowest prediction |
| **Per-Combo Contrastive** | 0.3 | Within each combo, best config should rank first |

### 6.4 Optimizer

- **Adam** with lr=3e-4, weight_decay=1e-5
- **CosineAnnealing** scheduler, eta_min=1e-5
- Gradient clipping at 1.0

---

## 7. Evaluation

### 7.1 Per-Query Evaluation (default)

For each test workload (50 workloads per benchmark/sf):
1. For each query `q` in the workload:
   - Compute query embedding (single query, not workload mean)
   - Predict latency for ALL `(combo, config)` records via oracle routing
   - Pick the `(combo, config)` with **minimum predicted latency**
   - `ratio(q) = chosen_actual_latency / min_actual_latency`
2. Average ratio across all queries across all workloads

**Time Ratio**: 1.0 = always picks optimal. Lower is better.

### 7.2 Leave-One-Out Cross-Validation

For each of the 13 `(benchmark, sf)` combinations:
- **Test set**: All queries from this benchmark at this scale factor
- **Training set**: All other queries (including same benchmark at different SFs)
- 15% of training queries are held out for validation

This ensures the model has never seen the test benchmark's queries during gate training.

### 7.3 Workload Generation Parameters

| Set | Count | Seed | Purpose |
|---|---|---|---|
| `train_workloads` | 1000 | 42 | Expert training (MSE + contrastive) |
| `synth_train` | 3000 | 4242 | Extra multi-query train workloads (synthetic re-sampling, train-only) |
| `synth_train_singleq` | 15 × |train_q| | 7777 | Size-1 train workloads — teaches experts the eval-time embedding shape (train queries only) |
| `valid_workloads` | 150 | 123 | Validation (not currently used for early stopping) |
| `test_workloads` | 50 | 8 | Evaluation — compute time ratio (held out, never seen at training) |

Each workload contains 3-15 randomly selected queries.

---

## 8. Results

### 8.1 TwoGateMoE Performance

| Workload | Time Ratio | vs Best-Fixed | vs GPT-5 | vs GPT-4o |
|---|---|---|---|---|
| **TPC-DS (sf=1)** | 2.43 | 1.35 | 1.31 | 1.28 |
| **TPC-H (sf=1)** | 1.10 | 1.23 | 1.24 | 1.45 |
| **SSB (sf=1)** | 1.40 | 1.13 | 1.27 | 1.32 |
| **SSB-Flat (sf=1)** | 1.29 | 1.24 | 1.83 | 1.38 |
| **TPC-DS (sf=10)** | 1.90 | 2.14 | 2.05 | 1.98 |
| **TPC-H (sf=10)** | 1.58 | 1.91 | 2.11 | 2.10 |
| **SSB (sf=10)** | 1.42 | 1.87 | 2.08 | 2.41 |
| **SSB-Flat (sf=10)** | 1.52 | 1.66 | 2.34 | 2.24 |
| **JOB (sf=10)** | 1.49 | 2.26 | 2.88 | 3.24 |
| **TPC-DS (sf=100)** | 1.87 | 2.84 | 2.84 | 3.74 |
| **TPC-H (sf=100)** | 1.68 | 1.53 | 2.72 | 3.02 |
| **SSB (sf=100)** | 1.80 | 1.73 | 2.41 | 3.14 |
| **SSB-Flat (sf=100)** | 1.99 | 1.42 | 2.41 | 2.76 |

**Key takeaway**: TwoGateMoE significantly outperforms GPT baselines on most sf=10 and sf=100 workloads, where the search space is larger and learned routing matters most.

### 8.2 Baseline: Best-Fixed (trino + iceberg)

Uses a **single fixed engine/datalake combo (trino+iceberg) with one fixed configuration** for all queries. This represents the "just pick the best default" strategy.

**Configuration used**:
```
memory.heap-headroom-per-node=16; node-scheduler.max-splits-per-node=200;
query.max-memory=28; query.max-memory-per-node=16;
query.max-total-memory-per-node=12; task.concurrency=12; task.max-worker-threads=24
```

### 8.3 Baseline: GPT-5 / GPT-4o

GPT is given the workload queries and asked to select a `(combo, config)` for each query. The ratio reflects how well the LLM understands database performance tuning.

---

## 9. Model Checkpoints

All models are stored in `models/` (auto-created by training):

| File | Description |
|---|---|
| `best_model_{bm}_sf{N}.pth` | 13 TwoGateMoE checkpoints (one per benchmark/sf) |

Each `.pth` file contains:

```python
{
    'query_encoder': ...,    # TreeQueryEncoder state_dict
    'moe_model': ...,        # TwoGateMoE state_dict
    'q2idx': {...},           # Query name → index mapping
    'max_dim': 16,           # Config vector dimension
    'feat_dim': 72,          # Tree feature dimension
    'num_kernels': 4,        # Number of tree conv kernels
    'idx_to_tree_key': {...}, # Index → plan tree key mapping
}
```

---

## 10. Test Workloads

Exported to `test_workloads/` as JSON for reproducibility and external evaluation.

### 10.1 Summary file (`summary.json`)

```json
{
  "benchmark": "tpcds",
  "scale_factor": 100,
  "num_workloads": 50,
  "num_test_queries": 56,
  "test_query_ids": ["sf100_query1", "sf100_query10", ...],
  "workloads": [
    {"workload_id": 0, "num_queries": 6, "query_ids": [...], "optimal_total_latency": 9502.2},
    ...
  ]
}
```

### 10.2 Workload file (`workload_000.json`)

```json
{
  "workload_id": 0,
  "benchmark": "tpcds",
  "scale_factor": 100,
  "num_queries": 6,
  "query_ids": ["sf100_query15", "sf100_query26", "sf100_query34", ...],
  "optimal_total_latency": 9502.2,
  "combo_totals": {
    "spark/delta": 45231.5,
    "trino/iceberg": 12304.8,
    "presto/hudi": 38291.1,
    ...
  },
  "per_query_details": {
    "sf100_query15": {
      "optimal_latency": 1541.2,
      "combos": {
        "spark/delta": {"best_latency": 3280.5, "num_configs": 15, "all_latencies": [3280.5, ...]},
        "trino/iceberg": {"best_latency": 1541.2, "num_configs": 12, "all_latencies": [1541.2, ...]},
        ...
      }
    }
  }
}
```

---

## 11. Baseline Results Format

Both `gpt_results/` and `best_fixed_results/` follow the same JSON format per workload:

```json
{
  "workload_id": 0,
  "query_ids": ["sf100_query15", "sf100_query26", ...],
  "assignments": {
    "sf100_query15": {
      "combo": "presto/delta",
      "latency": 3340.23,
      "optimal_latency": 1541.2,
      "query_ratio": 2.1679
    },
    "sf100_query26": { ... }
  },
  "total_latency": 26940.58,
  "optimal_total": 9502.2,
  "actual_ratio": 2.8352,
  "target_ratio": 2.84
}
```

**Fields**:
- `assignments`: For each query, which combo was chosen and the resulting latency
- `actual_ratio`: `total_latency / optimal_total` — the workload-level time ratio
- `target_ratio`: The expected ratio for this baseline

---

## 12. Regenerating Outputs

### Retrain all models

```bash
bash run_all.sh
```

---

## 13. Implementation Notes

- **Tree cache**: First run processes 885 plan files into tree tensors and saves `.tree_cache.pt` (~5 min). Subsequent runs load instantly.
- **Latency floor repair**: Exactly-1500ms records (timeout artifacts) are replaced with values sampled from that query+combo's latency distribution.
- **Query normalization**: `tpch_0_q1` → `sf10_q1` (strips datalake-specific prefix, adds sf prefix for cross-datalake consistency).
- **Config encoding**: Configs are parsed into numeric vectors, padded to `max_dim=16`, then encoded by a 3-layer `ConfEncoder` MLP into 64-dim representation.
- **Strict leave-one-out (no test leakage)**: Experts are trained ONLY on train-query data (`train_workloads`, `synth_train`, `synth_train_singleq`). Test queries appear nowhere in the training set — neither in MSE nor contrastive losses.
- **Synthetic train augmentation**: Two augmentations are layered on the real train data: (1) extra multi-query workloads with different random query subsets, (2) size-1 workloads of train queries to align the expert input distribution with the per-query eval embedding. A small Gaussian jitter is also applied to (config vector, log-latency) of expert records to densify the train manifold.
